name: 5-minute-cron

on:
    schedule:
        - cron '*/15 * * * *'

jobs:
    build:
        runs-on: ubuntu-latest
        
        steps:
            - name: Git checkout
            uses: actions/checkout@v2
            with: { ref: debug }
            - name: List files
            run: ls

